# 🤖 Настройка Antigravity Agent для проекта

## ✅ Что настроено

Создана конфигурация в директории `.agent/`, которая автоматически напоминает Antigravity Agent использовать виртуальное окружение.

## 📁 Структура конфигурации

```
.agent/
├── README.md                    # Критические правила проекта
└── workflows/
    ├── run-python.md           # Как запускать Python скрипты
    └── setup.md                # Как настроить виртуальное окружение
```

## 🎯 Как это работает

### Автоматические напоминания

Когда Antigravity Agent работает с этим проектом, он:
1. ✅ Видит workflows и автоматически следует им
2. ✅ Понимает, что нужно использовать `venv`
3. ✅ Использует wrapper-скрипты `./run.sh` и `./run_inference.sh`

### Доступные команды

Вы можете явно вызывать workflows через slash-команды:

- **`/run-python`** — показывает, как правильно запускать Python скрипты
- **`/setup`** — показывает, как настроить виртуальное окружение

## 📝 Примеры использования

### Для вас (пользователя):

```bash
# Быстрый запуск main.py
./run.sh

# Быстрый запуск inference.py  
./run_inference.sh

# Или вручную активировать venv
source venv/bin/activate
python main.py
```

### Для Antigravity Agent:

Теперь при запуске любого Python скрипта агент будет:
1. Проверять наличие `venv/`
2. Использовать `source venv/bin/activate` перед запуском
3. Или использовать wrapper-скрипты

## ⚙️ Технические детали

### Workflow файлы

Workflows с аннотацией `// turbo-all` позволяют агенту **автоматически** выполнять команды без запроса подтверждения.

### Почему это важно

- Системный Python 3.9.6 → **segmentation fault** ❌
- Venv Python 3.13.7 → **всё работает** ✅

## 🔄 Синхронизация с Git

Директория `.agent/` **включена** в репозиторий (см. `.gitignore`), поэтому:
- Конфигурация сохраняется между сессиями
- Другие разработчики получат те же настройки
- Antigravity Agent всегда будет знать правила проекта
